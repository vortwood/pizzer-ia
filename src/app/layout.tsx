import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Pizzaology - La Mejor Pizza Italiana",
  description: "Descubre el auténtico sabor italiano con nuestras pizzas artesanales. Ingredientes frescos, masa fermentada 48h y horneadas en horno de leña.",
  keywords: ["pizza", "italiana", "artesanal", "delivery", "restaurante"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
